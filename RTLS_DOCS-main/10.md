You raise a very important point. Let me lay out the numbers clearly:

### Your Actual Results

| Metric | Value | What it means |
|--------|-------|---------------|
| Goal reached rate | 93.9% | Planner reached within 1cm of target |
| Robo3D collision-free rate | 82.5% | Passed geometric capsule check (lenient) |
| **Robo3D validated success** | **80.3%** | Goal reached AND Robo3D collision-free |
| MoveIt collision-free rate | 49.4% | Passed full mesh FCL check (strict) |
| **MoveIt validated success** | **46.3%** | Goal reached AND MoveIt collision-free |

### The Author Reports: 85.8%

**We don't know which collision checker the author used for that number.** This is a critical ambiguity:

- If the author used **MoveIt** (full mesh) → your comparable number is **46.3%**, and the gap is **~40 percentage points** (massive)
- If the author used **Robo3D** (geometric capsules from `/sim`) → your comparable number is **80.3%**, and the gap is **~5.5 percentage points** (modest)

The fact that the author recommended you use the geometric checker from `/sim` suggests they *might* have used it themselves — but we can't be sure. The baselines in the paper (RRTConnect 84.9%, RRT* 85.0%, etc.) are all run through MoveIt/OMPL, which uses MoveIt's collision checking, so there's a question of consistency.

This needs to go into the mail. Here's the updated draft:

---

**Subject: RTLS: Evaluation Implementation Details — Seeking Clarification on Parameters for Reproducibility**

Dear Dr. Hung,

Thank you very much for your previous reply regarding the use of the geometric collision checker vs. the learned classifier for evaluation. We have implemented that change — using the capsule-based geometric checker from `/sim` for path validation (Stage 2), and the learned classifier only during planning (Stage 1) to provide differentiable gradients. We appreciate your guidance on that.

We are continuing to work on reproducing the results reported in the paper. However, we observe a significant discrepancy between the two collision checkers available to us:

- Using the **geometric capsule checker** (`Robo3D.check_for_collision` from `/sim`): **~80% success rate** (1 obstacle, 1000 scenarios)
- Using **MoveIt's FCL-based collision checker** (full mesh, same scenarios): **~46% success rate**

The geometric capsule checker is substantially more lenient than MoveIt — nearly half the paths that pass Robo3D validation fail MoveIt validation. This raises a critical question regarding the **85.8% success rate** reported in the paper (Table II, 1 obstacle):

**Which collision checker was used to validate the planned paths for the results in Table II?** Was it:
- (a) The geometric capsule-based checker from the `/sim` scripts (Robo3D), or
- (b) MoveIt's FCL-based collision checker (which was also used for the baseline planners like RRTConnect, RRT*, etc.), or
- (c) Some other method?

This distinction is crucial for us because our comparable number is either ~80% or ~46% depending on the answer, and it would fundamentally change our understanding of where the performance gap lies.

Additionally, we would greatly appreciate your clarification on the following aspects:

### 1. Obstacle Distance from Robot Base

The paper (Section IV-B) states: *"for each obstacle, we sample a distance to origin L, an angle θ_obs in [0, 2π) uniformly."*

- **What range was used for L?** Specifically, what was the **minimum distance** from the robot base at which obstacles could be placed during evaluation scenario generation?
- We have found that this parameter significantly impacts results — using a minimum distance of 0.2m gives substantially lower performance (~60%), while 0.4m gives results much closer to those reported in the paper. This suggests L_min is a critical but undisclosed parameter.
- Was the same L range used for both **training data generation** and **evaluation scenario generation**?

### 2. Obstacle Dimension Ranges

The paper does not specify the ranges for obstacle height and radius. Could you share:
- The **height range** of the cylindrical obstacles used during evaluation? (We use [0.5m, 1.0m])
- The **radius range** of the cylindrical obstacles used during evaluation? (We use [0.05m, 0.15m])
- Were these the same ranges used for both training data collection and evaluation?

### 3. Scene Generation Details

We generate evaluation scenes using MoveIt, validating that start and goal configurations are free of self-collision, table collision, and obstacle collision before accepting a scenario. We also ensure the end-effector is above the table surface. Could you confirm whether you used a similar validation approach? Additionally:
- Was there a **minimum or maximum distance between start and goal EE positions**?
- Were there any other scene filtering criteria we might be missing?

### 4. Planning Hyperparameters

The paper mentions a grid search was conducted on GECO parameters for obstacle avoidance (Section V-B). Could you share the values found through this grid search:
- The **Activation Maximisation learning rate (α_AM)** used during the final evaluation? (The paper mentions α_AM = 0.03 for AM evaluation during training in Section IV-A; was the same value used for the main obstacle avoidance evaluation, or was a different value used?)
- Were **λ_prior and λ_obs fixed** during planning, or were they **adapted using GECO**?
- If GECO was used, what were the **GECO target values (τ)** for the prior loss and obstacle loss terms?
- What were the **GECO smoothing factor** and **GECO learning rate** for the planning-time GECO?
- Was any **temperature scaling** applied to the collision classifier's sigmoid output during planning?

### 5. Collision Validation Details

For the final evaluation results reported in Table II:
- Was collision checked at **every optimisation step** (i.e., every decoded waypoint), or were intermediate configurations **interpolated** between consecutive waypoints and also checked?
- Was any **collision padding / safety margin** added to the capsule radii in the geometric checker?
- The paper states: *"The model is evaluated on scenarios in which it would collide with the obstacles if the obstacle loss term was not added."* Does this mean scenarios were **pre-filtered** to only include those where planning without obstacle avoidance would result in collision? If so, how was this filtering implemented?

### 6. Collision Loss Formulation for Multiple Obstacles

The paper (Eq. 6) shows the obstacle loss as: `λ_obs * Σᵢ(-log(1 - p_ϑ(z, oᵢ)))`. For multi-obstacle scenarios, was this loss **summed** over all obstacles as written, or was it **averaged** (divided by the number of obstacles)?

We understand this is a detailed list of questions, and any clarification you can provide — even partial — would be immensely helpful for our reproducibility effort. We are committed to correctly implementing the evaluation pipeline and would be grateful for any additional insights you may have.

Thank you very much for your time and support.

Best regards,
[Your Name]

---

**Key change from the previous draft**: The collision checker question is now the **lead question** with concrete numbers (80% vs 46%) showing why it matters. This is arguably the most critical unknonwn — it determines whether your gap is 5% or 40%.
